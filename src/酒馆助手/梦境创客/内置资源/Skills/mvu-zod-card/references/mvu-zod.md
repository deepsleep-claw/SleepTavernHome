# MVU Zod要点

- Schema负责校验和默认值；更新函数负责状态变化；渲染层只消费状态。
- 对可增长数组、数值范围、枚举和可选字段给出明确上限或默认值。
- Schema演进时提供可重复执行的迁移，不依赖“模型应该会补齐”。
- 不把完整UI HTML塞进世界书；世界书描述变量语义和更新契约即可。
- 需要接口类型时读取酒馆助手API参考Skill中的对应 `.d.ts`，不要凭记忆猜签名。
