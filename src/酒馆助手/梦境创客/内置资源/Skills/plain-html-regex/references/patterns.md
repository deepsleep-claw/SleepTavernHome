# 纯HTML模式

推荐根节点带唯一类名，例如 `.dream-status-card`。所有CSS选择器从根类开始，避免污染消息页面。

需要把捕获组作为正文时，优先让正则只替换明确的标记块。避免匹配整条消息、贪婪跨楼层或依赖不稳定DOM层级。

对长表格和代码：外层使用 `overflow:auto`；图片使用 `max-width:100%;height:auto`；移动端不固定像素宽度。
